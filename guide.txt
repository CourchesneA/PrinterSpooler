Use shm_open to open a shared memory and pass in a struct
    /thisisaname
use mmap to link

use shm_unlink to remove an object from memory


PRINTER_SERVER(bufferSize):
-Check buffer undeflow
-Create and maintain shared buffer in shared mem
-Monitor under/overflow
-Proceed jobs in the queue
-setup_shared_mem();
-attach_shared_mem();
-init_semaphore();  //In shared mem
-While(1){takejob,printmsg,sleep}

PRINT_CLIENT(jobDuration):
-Check buffer overflow
-Generate job on the print queue
-attach_shared_mem();
-get_job_params();  //get input from terminal
-create_job();
-pub job();
realease_share_mem();
